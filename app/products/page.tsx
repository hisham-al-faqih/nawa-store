'use client'

import Image from 'next/image'
import Link from 'next/link'
import { useMemo, useState } from 'react'
import { ArrowLeft, Heart, Search, ShoppingBag, SlidersHorizontal } from 'lucide-react'
import { catalogProducts, formatPrice } from '@/lib/catalog'

export default function ProductsPage() {
  const [query, setQuery] = useState('')
  const [category, setCategory] = useState('الكل')
  const [liked, setLiked] = useState<string[]>([])
  const categories = ['الكل', ...new Set(catalogProducts.map((product) => product.category))]
  const filtered = useMemo(() => catalogProducts.filter((product) => product.name.includes(query) && (category === 'الكل' || product.category === category)), [query, category])

  return <main dir="rtl" className="min-h-screen bg-[#FFFDFB] text-[#2f2728]"><header className="border-b border-[#eadfd9] bg-white/90 px-4 py-5"><div className="mx-auto flex max-w-7xl items-center justify-between"><Link href="/" className="text-xl font-bold text-[#773441]">سكون</Link><Link href="/" className="text-sm font-bold text-[#773441]">العودة للرئيسية <ArrowLeft className="mr-1 inline" size={15} /></Link></div></header><div className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8"><div className="mb-8"><p className="mb-2 text-xs font-bold tracking-[0.2em] text-[#a48682]">اكتشف تشكيلتنا</p><h1 className="text-3xl font-bold text-[#3c2b2d]">كل المنتجات</h1></div><div className="mb-8 flex flex-col gap-3 rounded-3xl border border-[#eadfd9] bg-white p-4 sm:flex-row"><label className="flex flex-1 items-center gap-3 rounded-2xl bg-[#f8f1ed] px-4"><Search size={18} className="text-[#9b7d77]" /><span className="sr-only">البحث عن منتج</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="ابحث عن سماعات، حقائب، عطور..." className="w-full bg-transparent py-3 text-sm outline-none placeholder:text-[#a48682]" /></label><div className="flex items-center gap-2 overflow-x-auto"><SlidersHorizontal size={17} className="text-[#773441]" />{categories.map((item) => <button key={item} onClick={() => setCategory(item)} className={`whitespace-nowrap rounded-full px-4 py-2 text-xs font-bold transition ${category === item ? 'bg-[#773441] text-white' : 'bg-[#f8f1ed] text-[#806e6b] hover:bg-[#f1ded0]'}`}>{item}</button>)}</div></div><p className="mb-5 text-sm text-[#806e6b]">{filtered.length} منتجات متاحة</p><div className="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4 sm:gap-5">{filtered.map((product) => <article key={product.slug} className="group"><Link href={`/products/${product.slug}`}><div className="relative aspect-[0.88] overflow-hidden rounded-3xl bg-[#f0ebe8]"><Image src={product.image} alt={product.name} fill className="object-cover transition duration-500 group-hover:scale-105" sizes="(max-width: 640px) 50vw, 25vw" /><span className="absolute right-3 top-3 rounded-full bg-white/85 px-2.5 py-1 text-[10px] font-bold text-[#773441]">{product.badge}</span></div></Link><div className="px-1 pt-3"><div className="flex items-start justify-between gap-2"><div><p className="text-[11px] text-[#a48682]">{product.category}</p><Link href={`/products/${product.slug}`} className="mt-1 block text-sm font-bold text-[#493638] hover:text-[#773441]">{product.name}</Link></div><button aria-label={`إضافة ${product.name} للمفضلة`} onClick={() => setLiked((items) => items.includes(product.slug) ? items.filter((item) => item !== product.slug) : [...items, product.slug])} className={`rounded-full p-1.5 ${liked.includes(product.slug) ? 'text-[#773441]' : 'text-[#a48682]'}`}><Heart size={16} fill={liked.includes(product.slug) ? 'currentColor' : 'none'} /></button></div><div className="mt-2 flex items-center justify-between"><span className="font-bold text-[#773441]">{formatPrice(product.price)} <small className="font-normal">ريال</small></span><span className="text-xs text-[#9b7d77]">★ {product.rating}</span></div></div></article>)}</div></div></main>
}
